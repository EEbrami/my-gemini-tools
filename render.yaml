services:
  - type: web
    name: academic-tool-server
    plan: free
    runtime: python
    pythonVersion: "3.11"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn my_academic_tools:app --host 0.0.0.0 --port $PORT"
